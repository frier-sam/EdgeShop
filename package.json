{
  "name": "edgeshop",
  "private": true,
  "workspaces": ["worker", "frontend"],
  "scripts": {
    "dev:worker": "cd worker && npm run dev",
    "dev:frontend": "cd frontend && vite",
    "deploy:worker": "cd worker && npm run deploy",
    "deploy:frontend": "cd frontend && npm run build && cd ../worker && node_modules/.bin/wrangler pages deploy ../frontend/dist --project-name edgeshop"
  }
}
